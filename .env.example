# Database
DATABASE_URL=./thunderdome.db

# GitHub App (handles both login AND repo access)
# Create at github.com/settings/apps/new
# Permissions: Contents (Read & Write), Metadata (Read)
# Enable "Request user authorization (OAuth) during installation"
GITHUB_APP_CLIENT_ID=Iv1.xxxx         # Client ID (starts with Iv1. or Iv23.)
GITHUB_APP_CLIENT_SECRET=             # Client secret (for OAuth login)
GITHUB_APP_PRIVATE_KEY=               # Base64 encoded .pem: cat key.pem | base64 -w0
GITHUB_APP_WEBHOOK_SECRET=            # Optional: for webhook signature verification

# NextAuth.js
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
AUTH_TRUST_HOST=true  # Required for production behind proxy

# Encryption (for storing sensitive tokens)
ENCRYPTION_KEY=

# Container runtime socket (Docker or Podman)
# Default: /var/run/docker.sock
# Podman: /run/podman/podman.sock
CONTAINER_SOCKET=
